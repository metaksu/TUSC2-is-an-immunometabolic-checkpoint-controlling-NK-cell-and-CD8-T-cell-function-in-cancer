"""Publication-ready Fig 7 panels for the TUSC2 DEG side-project.

Each panel renders independently (no collage) from the cached pipeline tables
to vector PDF + 600-dpi PNG, in a clean publication style with no on-panel
clutter. Panel letters, titles, and stat callouts are deliberately omitted;
they are added when the panels are assembled into the figure.
"""
